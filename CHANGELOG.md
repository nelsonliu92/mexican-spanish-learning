# CHANGELOG.md - 墨西哥西語學習 App

## [v0.6.0] - 2026-09-21

### 新增功能
- **🔤 基礎發音模組**：新增完整發音教學入口，與「每週學習」「我的單字本」並列為三大功能
  - **字母表**：27 個西語字母的互動卡片，點擊可朗讀字母名稱與示例單字
  - **特殊字母**：CH / LL / RR 的墨西哥發音特色說明
  - **母音專區**：A、E、I、O、U 五個母音的發音提示 + 示例朗讀
  - **雙母音**：ai/ay、ei/ey、oi/oy、au、eu、ia、ie、io 等常見雙母音組合
  - **子音規則**：C/G 軟硬音、R/RR 彈舌、J/G/H 區別、B/V 同音等 5 大規則
  - **重音規則**：以母音/N/S 結尾、以子音結尾、重音符號 tilde、問號驚嘆號反轉
  - 全部發音示例均附帶點擊朗讀（TTS es-MX）
  - 四個頁籤切換，手機 RWD 自適應

### 修正
- **字母卡片發音順序**：點擊字母卡片時，先朗讀字母本身（如「a」），再朗讀字母名稱（如「a」），最後朗讀示例單字（如「amigo」）
- **字母卡片內容**：卡片上顯示示例單字與中文翻譯，取代原本的發音提示縮寫

---

## [v0.5.3] - 2026-09-21

### 新增功能
- **Favicon**：新增 SVG 內嵌 favicon（墨西哥國旗 🇲🇽 + 暖白背景 + 圓角），瀏覽器分頁與書籤顯示可愛圖示

---

## [v0.5.2] - 2026-09-21

### 新增功能
- **GitHub Pages 線上部署**：專案已部署至 https://nelsonliu92.github.io/mexican-spanish-learning/
  - iOS Safari / 任何瀏覽器開啟，**localStorage 正常持久化**，進度不會丟失
  - 無需手動 JSON 備份

### 修復
- **主頁進度首次載入不更新**：頁面載入時自動執行 `load();loadVocab();updateHomeProgress();updateVocabBadge()`
- **單字本數量首次載入不更新**：`showVocab()` / `showHome()` 函數增加 `loadVocab()` 前置呼叫
- **頁面首次開啟資料不載入**：Script 結尾增加初始化呼叫，避免需等點擊課程才觸發 `init()`

### 移除
- **🗑️ 進度備份功能**：因部署至 GitHub Pages 後 localStorage 正常運作，手動 JSON 備份已非必要，移除主頁卡片、JS 函數、模態窗與 CSS

---

## [v0.5.1] - 2026-09-21

### 新增功能
- **💾 進度備份與匯入**：解決 iOS Safari `file://` 模式下 `localStorage` 會在 App 重開後被清除的問題
  - 主主頁新增「💾 進度備份」入口卡片
  - **匯出**：按鈕產生 JSON 備份碼（內含 `progress` + `vocab`），自動選取文字由使用者手動複製，貼到備忘錄保存
  - **匯入**：貼上之前存的 JSON → 按「匯入進度」→ 自動恢復學習進度與單字本
- **檔名統一**：取消 `español.html`（Unicode 文件名在 Windows/NFS 與 iOS 間有 NFD/NFC 編碼差異），統一使用 `index.html`

### 修復
- **Script Error 修復（備份功能）**：上一輪 `backup_feature.py` 因 Unicode 文件名解碼錯誤導致操作到不存在的檔案，已從 v0.5.0 乾淨備份重新啟動

---

## [v0.5.0] - 2026-09-21

### 新增功能
- **🎙️ 發音按鈕補強**：字典彈窗新增「🔊 朗讀」按朗讀」按鈕，點擊查詞後可直接朗讀該單字（`es-MX`）
- **📝 每日小測驗擴展為 3 題**：每 Day 從 1 題擴充為 3 題連續作答，答完自動顯示「下一題 →」，3 題全對才計入完成
- **⭐ 我的單字本**：
  - 字典彈窗新增「☆ 加入單字本 / ★ 已收藏」按鈕
  - 主頁新增「🌍 我的單字本」入口卡片（顯示收藏數量）
  - 單字本頁面支援朗讀與刪除，使用 `localStorage`（key: `mx_vocab_notebook_v1`）離線儲存
- **測驗解析強化**：每題答錯即時顯示解釋，3 題結束顯示總結（全對顯示 🎉 / 未全對提示可以再練習）
- **📊 主頁進度視覺化**：
  - Day 1~7 加上 `id`（`home-day-1` ~ `home-day-7`）
  - 新增 `.done`（綠色）與 `.failed`（紅色）CSS class
  - 新增 `updateHomeProgress()` 動態更新每個 day row 狀態與進度條
  - 測驗完成後回主頁自動顯示 ✅（全對）或 ❌（答錯）
- **🔄 重新測驗**：答錯的 Day 可點「重新測驗」清除進度再次挑戰

### 修復
- **Script Error（LESSONS quiz 格式）**：Python 批次腳本 `apply_all.py` 只改題目變數名但沒同步改 `LESSONS` 內引用，導致 `ReferenceError`。已用 `fix_lessons2.py` 修為 `quiz:[Q1_1,Q1_2,Q1_3]`
- **HTML 實體編碼亂碼**：`&#191;`、`&#233;` 實體被 `esc()` 轉義為 raw 字串顯示。已用 `html.unescape()` 全檔案批量轉換為真實 Unicode 字符

---

## [v0.4.7] - 2026-09-??

### 基礎功能
- 單頁 SPA 架構（主頁 + 7 Day 課程 + 單字本頁面切換）
- 每日例句含墨西哥地道用語
- 可點擊查詞字典彈窗（中文解釋 + 例句）
- 線上 TTS 語音朗讀（`es-MX`）
- `localStorage` 儲存學習進度（`mx_spanish_progress_v1`）
- 每日小測驗（3 選 1 選擇題）